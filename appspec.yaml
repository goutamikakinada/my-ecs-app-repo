version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: {"Ref": "TaskDefinition"}
        LoadBalancerInfo:
          ContainerName: "my-ecs-app-container"
          ContainerPort: 3000

